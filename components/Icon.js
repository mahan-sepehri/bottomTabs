import { createIconSetFromIcoMoon } from "@expo/vector-icons";

const Icon = createIconSetFromIcoMoon(
  require("../assets/iconFont/selection.json"),
  "IcoMoon",
  "icomoon.ttf"
);

export default Icon;
